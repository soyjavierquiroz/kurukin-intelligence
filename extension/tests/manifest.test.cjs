const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
const root=path.join(__dirname,'..'),manifest=require('../manifest.json');
test('MV3 0.8.9 declares the production HTTPS backend and no automatic ML scripts',()=>{assert.equal(manifest.name,'Kurukin Intelligence v0.8.9');assert.equal(manifest.short_name,'Kurukin v0.8.9');assert.equal(manifest.version,'0.8.9');assert.deepEqual(manifest.host_permissions,['https://intelligence.kuruk.in/*']);assert.match(manifest.content_security_policy.extension_pages,/connect-src https:\/\/intelligence\.kuruk\.in/);const main=manifest.content_scripts[0].js.join('\n');for(const forbidden of ['vad-web','ort-wasm-1.22.0','audio-classifier/classifier','page/speech'])assert.equal(main.includes(forbidden),false,forbidden);assert.ok(manifest.content_scripts[1].js.includes('lib/wav-handoff.js'));assert.ok(manifest.content_scripts[1].js.includes('lib/acquisition.js'));assert.ok(manifest.content_scripts[1].js.includes('lib/audio-diagnostic.js'));assert.ok(fs.existsSync(path.join(root,'lib/backend.js')));});
test('experimental model code remains packaged but is web-accessible only for explicit dynamic loading',()=>{const resources=manifest.web_accessible_resources[0].resources;for(const file of ['lib/vad/silero-vad-legacy.onnx','lib/audio-classifier/yamnet.onnx','lib/vad/vad-web-0.0.29.min.js','lib/audio-classifier/classifier.js','page/speech.js'])assert.ok(resources.includes(file));});
test('all declared files exist and the relays have no persistent media store',()=>{const files=[manifest.background.service_worker,...manifest.content_scripts.flatMap(e=>[...e.js,...(e.css||[])]),...manifest.web_accessible_resources.flatMap(e=>e.resources)];for(const file of files)assert.ok(fs.existsSync(path.join(root,file)),file);for(const file of ['content/content.js','page/bridge.js','lib/acquisition.js']){const source=fs.readFileSync(path.join(root,file),'utf8');for(const forbidden of ['chrome.storage','localStorage','indexedDB','playAddr','downloadAddr'])assert.equal(source.includes(forbidden),false,`${file}: ${forbidden}`);}});
