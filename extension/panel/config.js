/*
 * Local presentation flag. Product builds keep this false. For the separate
 * operator package, set it to true before packaging; the curator engine and
 * all extension permissions remain identical.
 */
globalThis.KurukinPanelConfig=Object.freeze({ADMIN_DEBUG_MODE:false,EXTENSION_VERSION:'0.8.11'});
